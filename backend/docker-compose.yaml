version: "3.7"

services:
  my-backend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/myfolder/src
    ports:
      - "3000:3000"

  my-database:
    image: mysql:latest
    enviornment:
      MYSQL_DATABASE: "my_books_docker"
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_USER: "user"
      MYSQL_PASSWORD: "password"
    prorts:
      - 3306:3306
